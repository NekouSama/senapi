<form method="post" action="{{ path('application1_objectif_delete', {'id': application1_objectif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ application1_objectif.id) }}">
    <button class="btn">Delete</button>
</form>
